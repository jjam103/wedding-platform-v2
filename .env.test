# Test Environment Configuration
# This file is used for E2E and integration tests

# Supabase Configuration (use test instance or mock values)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key
SUPABASE_SERVICE_ROLE_KEY=test-service-role-key

# External Services (mock values for testing)
TWILIO_ACCOUNT_SID=test
TWILIO_AUTH_TOKEN=test
TWILIO_PHONE_NUMBER=+15555555555

# Email Service (mock values)
RESEND_API_KEY=test-resend-key

# Storage (mock values)
B2_APPLICATION_KEY_ID=test-b2-key-id
B2_APPLICATION_KEY=test-b2-key
B2_BUCKET_ID=test-bucket-id
B2_BUCKET_NAME=test-bucket

# AI Service (mock values)
GOOGLE_GEMINI_API_KEY=test-gemini-key

# Test-specific settings
NODE_ENV=test
E2E_BASE_URL=http://localhost:3000
