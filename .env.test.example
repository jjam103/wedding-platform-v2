# Test Environment Configuration
# Copy this file to .env.test and fill in your test database credentials

# Supabase Test Database
# Use a separate test database instance to avoid affecting development data
NEXT_PUBLIC_SUPABASE_URL=your-test-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-test-supabase-anon-key

# Note: Integration tests require a real Supabase instance
# You can use the same instance as development, but it's recommended
# to use a separate test project to avoid data conflicts
