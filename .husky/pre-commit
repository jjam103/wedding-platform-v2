#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run type checking
echo "📝 Type checking..."
npm run test:types || exit 1

# Run build to catch compilation errors
echo "🏗️  Building..."
npm run build || exit 1

# Run quick tests (without build since we just built)
echo "🧪 Running tests..."
npm run test:quick || exit 1

echo "✅ Pre-commit checks passed!"
